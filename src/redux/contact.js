
import * as ActionTypes from './ActionTypes';

export const Leaders = (status=[] , action) => {
    switch(action.type) {
        case ActionTypes.ADD_FEEDBACK:
        return status

        default:
            return state;
    }
}